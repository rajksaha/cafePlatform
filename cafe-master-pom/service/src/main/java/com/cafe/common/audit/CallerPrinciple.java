package com.cafe.common.audit;

/**
 * Created by mamun on 1/25/16.
 */
public class CallerPrinciple {

    /*private static final ThreadLocal<UserDetails> userThreadLocal = new ThreadLocal<UserDetails>() {
        @Override
        protected UserDetails initialValue() {
            return null;
        }
    };

    public static UserDetails getUser() {
        return userThreadLocal.get();
    }

    public static String getUsername() {
        UserDetails userDetails = getUser();
        return userDetails == null ? "anonymous" : userDetails.getUsername();
    }

    public static void setUser(UserDetails userDetails) {
        userThreadLocal.set(userDetails);
    }*/
}
